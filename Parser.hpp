#ifndef PARSER_HPP
#define PARSER_HPP
using namespace std;
#include <string>
#include <vector>
#include "VMtoAsm.hpp"

class Parser{
    public:
        void parse(string vmInput);
};
#endif
